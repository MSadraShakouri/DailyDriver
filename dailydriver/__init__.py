"""DailyDriver package metadata."""

# Keep the release version in the importable package metadata. ``pyproject.toml``
# exposes this value to packaging tools, and runtime callers can use the same
# value without maintaining a second application constant.
__version__ = "2.1.0"
