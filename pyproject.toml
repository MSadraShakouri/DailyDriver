[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "dailydriver"
version = "1.0.0"
description = "Personal terminal‑based life tracker (prayer, sleep, journal, calendar)"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.8"
dependencies = [
    "jdatetime",
    "hijridate",
]

[project.scripts]
daily = "dailydriver.cli.commander:repl"

[tool.setuptools.packages.find]
include = ["dailydriver*"]
